'use client'
import { useEffect, useState } from 'react'
import AppShell from '@/components/AppShell'; import DataTable from '@/components/DataTable'; import FormModal from '@/components/FormModal'; import { supabase } from '@/lib/supabase'; import { statusClass,money } from '@/lib/status'; import toast from 'react-hot-toast'
const estados=['recibido','diagnostico','en_proceso','pendiente_cliente','pendiente_material','terminado','entregado','cancelado']
export default function Expedientes(){ const [items,setItems]=useState<any[]>([]),[clientes,setClientes]=useState<any[]>([]),[vehiculos,setVehiculos]=useState<any[]>([]),[open,setOpen]=useState(false),[f,setF]=useState<any>({estado:'recibido',prioridad:'normal'}); useEffect(()=>{load()},[]); async function load(){const [{data:e},{data:c},{data:v}]=await Promise.all([supabase.from('expedientes').select('*,clientes(nombre),vehiculos(marca,modelo,matricula)').order('created_at',{ascending:false}),supabase.from('clientes').select('id,nombre').order('nombre'),supabase.from('vehiculos').select('id,marca,modelo,matricula').order('created_at',{ascending:false})]); setItems(e||[]); setClientes(c||[]); setVehiculos(v||[])} async function save(e:any){e.preventDefault(); const {error}=await supabase.from('expedientes').insert(f); if(error) toast.error(error.message); else{toast.success('OT creada'); setOpen(false); setF({estado:'recibido',prioridad:'normal'}); load()}}
return <AppShell><div className="flex justify-between mb-5"><h2 className="text-2xl font-black">Expedientes / OT</h2><button onClick={()=>setOpen(true)} className="btn btn-red">Nueva OT</button></div><DataTable columns={['OT','Cliente','Vehículo','Trabajo','Estado','Prioridad','Importe']} rows={items.map(i=>[<b>{i.numero_ot}</b>,i.clientes?.nombre,`${i.vehiculos?.marca||''} ${i.vehiculos?.modelo||''} ${i.vehiculos?.matricula||''}`,i.tipo_trabajo,<span className={`badge ${statusClass(i.estado)}`}>{i.estado}</span>,i.prioridad,money(i.precio_final||i.precio_estimado)])}/><FormModal open={open} onClose={()=>setOpen(false)} title="Nueva OT"><form onSubmit={save} className="grid md:grid-cols-2 gap-3"><select onChange={e=>setF({...f,cliente_id:e.target.value})}><option>Cliente</option>{clientes.map(c=><option key={c.id} value={c.id}>{c.nombre}</option>)}</select><select onChange={e=>setF({...f,vehiculo_id:e.target.value})}><option>Vehículo</option>{vehiculos.map(v=><option key={v.id} value={v.id}>{v.marca} {v.modelo} {v.matricula}</option>)}</select><input required placeholder="Tipo trabajo: clonación ECU, llave, stage..." onChange={e=>setF({...f,tipo_trabajo:e.target.value})}/><select value={f.estado} onChange={e=>setF({...f,estado:e.target.value})}>{estados.map(x=><option key={x}>{x}</option>)}</select><select onChange={e=>setF({...f,prioridad:e.target.value})}><option>normal</option><option>baja</option><option>alta</option><option>urgente</option></select><input placeholder="Técnico" onChange={e=>setF({...f,tecnico:e.target.value})}/><input placeholder="Precio estimado" type="number" onChange={e=>setF({...f,precio_estimado:Number(e.target.value)})}/><textarea className="md:col-span-2" placeholder="Descripción" onChange={e=>setF({...f,descripcion:e.target.value})}/><textarea className="md:col-span-2" placeholder="Notas internas" onChange={e=>setF({...f,notas_internas:e.target.value})}/><button className="btn btn-red md:col-span-2">Guardar OT</button></form></FormModal></AppShell>}
